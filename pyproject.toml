[project]
name = "gtfs-filtering"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click>=8",
    "duckdb>=1",
    "python-i18n[yaml]>=0.3",
    "PyQt6>=6",
    "pytest>=8.4.2",
    "fsspec>=2026.6.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6",
    "pytest>=8",
    "ruff>=0",
]
