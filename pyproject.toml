[project]
name = "gtfs-filtering"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = [
    "click>=8,<9",
    "pandas>=2,<3",
    "PyQt6>=6,<7",
]

[dependency-groups]
dev = [
    "pyinstaller>=6,<7",
    "pytest>=8,<9",
    "ruff>=0,<1",
]
